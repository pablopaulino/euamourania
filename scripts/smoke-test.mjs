const base=process.env.SITE_URL||"https://euamourania.com.br",routes=["/","/news/","/guia.html","/turismo.html","/eventos/","/links/","/quem-somos.html","/sitemap.xml","/news-sitemap.xml","/robots.txt","/admin/login.html","/admin/publicidade.html","/admin/comunicacao.html","/descadastrar.html"];const failures=[];for(const path of routes){try{const r=await fetch(base+path,{redirect:"follow"});if(!r.ok)failures.push(`${path}: HTTP ${r.status}`);else console.log(`✓ ${path} (${r.status})`)}catch(e){failures.push(`${path}: ${e.message}`)}}const protectedEndpoint=await fetch(base+"/api/newsletter-send",{method:"POST",headers:{"Content-Type":"application/json"},body:"{}"});if(protectedEndpoint.status!==401)failures.push(`/api/newsletter-send: esperado 401 sem autenticação, recebido ${protectedEndpoint.status}`);else console.log("✓ API de newsletter protegida");if(failures.length){console.error(failures.join("\n"));process.exit(1)}console.log("Smoke test concluído.");
